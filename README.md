# CloudEnv

CloudEnv is a repository that sets up a local dockerized kubernetes environment for development and testing purposes. It provides a simple way to spin up a Kubernetes cluster using Docker.

## Prerequisites
